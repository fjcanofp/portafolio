---
title: 'UT02 · Resolución de nombres: DNS con BIND9 en Debian 13'
description: 'DNS desde cero con Debian 13: BIND9, zonas directa/inversa, clientes, alias, correo, caché,
  primario/secundario y DDNS.'
summary: Implementa y diagnostica DNS autoritativo y recursivo en Debian 13 con progresión de nivel ASIR.
module_key: sri
cycle_key: asir
module_title: Servicios de Red e Internet
module_code: '0375'
cycle_title: Administración de Sistemas Informáticos en Red
course: 2.º ASIR
unit: UT02
level: intermedio
authors:
- fjcano
reviewers:
- fjcano
rights: all-rights-reserved
version: '1.0'
last_reviewed: '2026-09-23'
visibility: public
ra:
- RA1
ce:
- RA1.a
- RA1.b
- RA1.c
- RA1.d
- RA1.e
- RA1.f
- RA1.g
- RA1.h
- RA1.i
- RA1.j
published: false
order: 2
hours: 14
tags:
- dns
- bind9
- debian-13
- zonas
- directa
- inversa
- secundario
- cache
- ddns
permalink: /docencia/asir/sri/ut02/
toc:
- title: Misión
  id: inicio
- title: RA1 y CE
  id: ra-ce
- title: DNS desde cero
  id: conceptos
- title: Individualización y VM
  id: redes
- title: Preparación Debian
  id: preparacion
- title: Instalación BIND9
  id: instalacion
- title: Zona directa
  id: zonas
- title: Zona inversa
  id: inversa
- title: Cliente DNS
  id: cliente
- title: Secundario
  id: secundario
- title: Caché y reenviadores
  id: cache
- title: Delegación
  id: delegacion
- title: DDNS con Kea
  id: ddns
- title: Diagnóstico
  id: diagnostico
- title: Prácticas
  id: practicas
---

<a id="inicio"></a>
## Misión · del DHCP a los nombres

**Ahora sabemos obtener una IP mediante DHCP. El siguiente problema es saber cómo llamar a los servicios.** En UT02 instalamos, configuramos, comprobamos y diagnosticamos **DNS** primero con BIND9 en Debian 13 y luego con el rol DNS de Windows Server 2025. Son **dos laboratorios obligatorios del mismo RA1**, no una UT03 improvisada.

**Ruta mínima**: dos VM, zona directa, inversa, A, PTR, CNAME, MX y consultas explícitas. **Ruta profesional**: tres VM para primario/segundo, caché y reenviadores con conexión real, delegación y DDNS con Kea; no se mezclan todos los escenarios el primer día. Antes de añadir otra VM, crea snapshot.

> **Seguridad:** todo DNS y DHCP de pruebas opera en **Red Interna** de VirtualBox. NAT solo para instalar paquetes/probar reenviadores temporalmente. No compartas la LAN DHCP del aula con la infraestructura del centro.

<a id="ra-ce"></a>
## RA1 y criterios de evaluación completos

**RA1. Administra servicios de resolución de nombres, analizándolos y garantizando la seguridad del servicio.**

| CE | Qué debe demostrar el alumno |
|---|---|
| a | Identifica situaciones en que se necesita resolución de nombres. |
| b | Compara hosts, DNS local y sistema jerárquico. |
| c | Explica raíz, TLD, dominios, zonas y FQDN. |
| d | Instala/configura servidor y cliente DNS. |
| e | Configura un reenviador de recursos externos cuando haya conectividad. |
| f | Diferencia caché, TTL y respuestas autoritativas. |
| g | Añade A, PTR, NS, CNAME y MX. |
| h | Explica/ensaya DDNS controlado integrado con Kea. |
| i | Realiza transferencia entre primario y secundario. |
| j | Documenta diseño, pruebas, errores y recuperación. |

**Correspondencia con [RD 1629/2009, módulo 0375](https://www.boe.es/buscar/doc.php?id=BOE-A-2009-18355)**. Las calificaciones, soluciones y datos privados permanecen en Moodle.

<a id="conceptos"></a>
## 1 · DNS desde cero: qué ocurre cuando escribimos un nombre

DHCP responde «¿qué configuración de red uso?». DNS responde «¿qué datos están asociados con este nombre?». **DHCP no crea por sí mismo registros DNS**: anunciar un DNS por la opción 6 no garantiza que ese servidor tenga una zona correcta.

```text
Persona escribe www.p07-l03.sri.test
        ↓
Aplicación pregunta al resolvedor del sistema
        ↓
Cliente consulta servidor DNS configurado (UDP/TCP 53)
        ↓
Servidor devuelve CNAME y/o A si conoce la zona
        ↓
Aplicación utiliza dirección IP para abrir el servicio web
```

- **Nombre de host:** `web`. **FQDN:** `web.p07-l03.sri.test.`; el punto final representa la raíz (en la zona es importante).
- **Dominio:** `p07-l03.sri.test`. **Zona:** conjunto de registros que administra un servidor autoritativo.
- **Resolución directa:** nombre → IP (A, AAAA). **Inversa:** IP → nombre (PTR). No aparecen PTR mágicamente por crear A.
- **Autorizado/autoritativo:** el servidor mantiene registros de una zona. **Recursivo/cache:** busca o recuerda respuestas de otras zonas; se deben restringir los clientes autorizados.
- **TTL:** duración orientativa de caché de un RR; repetir `dig` contra un DNS no prueba acceso a Internet.
- **SOA:** autoridad/serial y temporizadores; **NS:** servidores autoritativos; **MX:** destino de correo, no IP; **CNAME:** alias; **TXT:** texto de política (SPF se verá en correo).
- **Hosts:** resolución local del SO, no publicación DNS; en Linux examina `/etc/nsswitch.conf` antes de atribuir todo a BIND.

**`dig @SERVIDOR nombre A`** pregunta al servidor indicado. **`getent hosts nombre`** utiliza el mecanismo del sistema. **`ping`** no es una prueba concluyente de DNS si ICMP o el servicio remoto no responde.

<a id="redes"></a>
## 2 · Laboratorio, números individualizados y dos máquinas

Para evitar copias: `P` = puesto 01–40; `L` = inicial normalizada del primer apellido A=01…Z=26. **Ejemplo didáctico P07-L03**; los alumnos calculan los suyos.

| Rol | Regla | Ejemplo P07-L03 |
|---|---|---|
| Red Linux | `10.37.P.0/24` | `10.37.7.0/24` |
| Kea de UT01 | `10.37.P.(20+L)` | `10.37.7.23` |
| DNS primario Debian | `10.37.P.(30+L)` | `10.37.7.33` |
| DNS secundario Debian | `10.37.P.(40+L)` | `10.37.7.43` |
| Web futuro | `10.37.P.(50+L)` | `10.37.7.53` |
| Mail futuro | `10.37.P.(60+L)` | `10.37.7.63` |
| Cliente inicial de UT00 | `10.37.P.(120+L)` | `10.37.7.123` |
| Zona por alumno | `pXX-lYY.sri.test` | `p07-l03.sri.test` |
| Red interna | `SRI-Pxx` | `SRI-P07` |

```text
Red Interna SRI-P07 (NO puente, NO DHCP VirtualBox)
   ├── dns1-p07-l03 Debian 13: 10.37.7.33/24 (IP fija)
   └── cli-p07-l03 Debian 13 o Windows 11: 10.37.7.123/24
Ampliación: dns2-p07-l03: 10.37.7.43/24
Integración DHCP: añadir Kea .23; no es requisito para zona directa básica.
```

**ATENCIÓN:** ahora estamos instalando un *servidor dedicado DNS* en `.33`, no sustituyendo ni cambiando la IP `.23` de Kea. Puedes clonar Debian BASE de UT00, generar MAC nuevas y cambiar IP/hostname antes de encender al lado de la original. Una IP dentro del pool no se asigna manualmente al DNS; `.33` queda fuera del pool. Por ahora el cliente puede permanecer manual, o utilizar una lease de Kea con opción 6 actualizada a `.33` cuando practiquemos integración.

<a id="preparacion"></a>
## 3 · Preparación de Debian 13, clic a clic

1. VirtualBox → **clonar** base Debian (o crear VM nueva), generar MAC distintas. Nombre `dns1-p07-l03`.
2. Adaptador 1 NAT **solo durante instalación**; Adaptador 2 Red interna `SRI-P07`. Verifica que NO sea puente.
3. Abre la consola Debian. `ip -br link` para identificar NIC interna real, por ejemplo `enp0s8`.
4. Comprueba gestor con `systemctl is-active networking`. Si `networking` gestiona tu NIC, usa `/etc/network/interfaces`, NO `nmcli` ni `networkd` a la vez.
5. `sudo nano /etc/network/interfaces`: conserva NAT intacta; para la NIC interna añade (solo ejemplo):

```text
auto enp0s8
iface enp0s8 inet static
    address 10.37.7.33
    netmask 255.255.255.0
```

6. Aplica **desde consola local**: `sudo systemctl restart networking`; valida `ip -br a` y `ip route`.
7. `sudo hostnamectl set-hostname dns1-p07-l03`. En `/etc/hosts` adapta la línea local si apuntaba al hostname antiguo; no confundas archivo hosts con una zona DNS.
8. Snapshot `DNS00_IP_OK`. El cliente usa `10.37.7.123/24` en el mismo `SRI-P07`.

Si `systemctl is-active networking` da inactivo, no copies esta ruta a ciegas: revisa las alternativas documentadas en [UT00](/docencia/asir/sri/ut00/).

<a id="instalacion"></a>
## 4 · Instalar BIND9 y aprender dónde está cada archivo

**En dns1, con NAT temporal funcional:**

```bash
sudo apt update
sudo apt install -y bind9 bind9-utils dnsutils
systemctl is-active named || systemctl is-active bind9
sudo named-checkconf
ip -br a
```

No se supone que `dig` exista antes de instalar `dnsutils`. Comprueba el estado real del servicio; en Debian algunas unidades tienen alias y el nombre visible depende del paquete. Archivos esenciales:

| Archivo | Para qué sirve |
|---|---|
| `/etc/bind/named.conf` | Incluye los demás ficheros BIND. |
| `/etc/bind/named.conf.local` | Declaraciones de zonas administradas. |
| `/etc/bind/named.conf.options` | Políticas generales, recursión y forwarders. |
| `/etc/bind/db.*` | Registros de zona **estáticos** editados por administrador. |
| `/var/cache/bind/` | Datos regenerables/zonas secundarias en configuración Debian habitual. |
| `/var/lib/bind/` | Zonas que BIND necesita modificar: caso DDNS. |

No se modifica `/etc/resolv.conf` sin revisar quién lo genera: puede sobrescribirse al reiniciar. Para comprobar un DNS explícito, usa `dig @10.37.7.33 ...`, independiente del resolver del propio servidor.

<a id="zonas"></a>
## 5 · Crear una zona directa autoritativa, paso a paso

**Paso A · respaldo:**

```bash
sudo cp /etc/bind/named.conf.local /etc/bind/named.conf.local.ut02.bak
sudo nano /etc/bind/named.conf.local
```

**Añade** (no borres otras declaraciones):

```conf
zone "p07-l03.sri.test" {
    type primary;
    file "/etc/bind/db.p07-l03.sri.test";
    allow-transfer { none; };
};
```

**Paso B · crear archivo de zona:**

```bash
sudo nano /etc/bind/db.p07-l03.sri.test
```

```dns
$TTL 3600
@   IN SOA ns1.p07-l03.sri.test. hostmaster.p07-l03.sri.test. (
        2026092301 ; serial: incrementar con cada cambio
        3600       ; refresh
        900        ; retry
        604800     ; expire
        3600 )     ; negative cache
@       IN NS       ns1.p07-l03.sri.test.
ns1     IN A        10.37.7.33
cli     IN A        10.37.7.123
web     IN A        10.37.7.53
mail    IN A        10.37.7.63
www     IN CNAME    web.p07-l03.sri.test.
@       IN MX 10    mail.p07-l03.sri.test.
```

**Razona:** NS y MX referencian **nombres**, no sustituyen A. `www` es alias, no otro servidor obligado a existir. Un registro A puede apuntar a un host aún sin servicio encendido: consulta DNS ≠ ping/HTTP exitoso.

**Paso C · validar ANTES de recargar:**

```bash
sudo named-checkconf
sudo named-checkzone p07-l03.sri.test /etc/bind/db.p07-l03.sri.test
sudo rndc reload p07-l03.sri.test
sudo journalctl -u named -u bind9 -b --no-pager -n 30
```

Si `rndc reload zona` no resulta operativo, consulta `sudo systemctl reload named` o `bind9` según nombre de unidad *real* y logs. Si ves `loaded serial 2026092301` es una pista útil, pero la prueba concluyente llega desde cliente.

<a id="inversa"></a>
## 6 · Zona inversa: que una IP tenga nombre

La IP `10.37.7.33` invierte octetos para llegar a la zona **`7.37.10.in-addr.arpa`**; en ella el owner `33` representa el último octeto.

**En `/etc/bind/named.conf.local` añade:**

```conf
zone "7.37.10.in-addr.arpa" {
    type primary;
    file "/etc/bind/db.10.37.7.rev";
    allow-transfer { none; };
};
```

**Crea `/etc/bind/db.10.37.7.rev`:**

```dns
$TTL 3600
@ IN SOA ns1.p07-l03.sri.test. hostmaster.p07-l03.sri.test. (
    2026092301 3600 900 604800 3600 )
@   IN NS   ns1.p07-l03.sri.test.
33  IN PTR  ns1.p07-l03.sri.test.
123 IN PTR  cli.p07-l03.sri.test.
```

**Validación independiente:**

```bash
sudo named-checkconf
sudo named-checkzone 7.37.10.in-addr.arpa /etc/bind/db.10.37.7.rev
sudo rndc reload
```

**Comprueba desde el CLIENTE** (instala `dnsutils` si es Debian y NAT para instalar):

```bash
dig @10.37.7.33 ns1.p07-l03.sri.test A +noall +answer
dig @10.37.7.33 www.p07-l03.sri.test CNAME +noall +answer
dig @10.37.7.33 p07-l03.sri.test MX +noall +answer
dig @10.37.7.33 -x 10.37.7.33 +noall +answer
```

En `dig` el flag `aa` indica respuesta autoritativa. La opción `@IP` permite diagnosticar sin tocar el DNS por defecto del cliente.

<a id="cliente"></a>
## 7 · Hacer que el cliente utilice nuestro DNS

**Primero probamos con `@10.37.7.33`; después configuramos el SO.**

**Debian con ifupdown:** agrega a la NIC interna `dns-nameservers 10.37.7.33` solo si el sistema dispone de integración `resolvconf`/gestión apropiada; no presupongas que todos los Debian mínimos actualizarán `resolv.conf` automáticamente. Comprueba `cat /etc/resolv.conf`, `resolvectl status` (solo si `systemd-resolved` activo) y `getent hosts ns1.p07-l03.sri.test`. Alternativa docente reproducible: cliente DHCP de UT01 anuncia opción 6 = `.33` y opción 15 = `p07-l03.sri.test`; renueva y contrasta ACK/estado del cliente.

**Windows 11:** Win+R → `ncpa.cpl` → NIC interna → Propiedades → TCP/IPv4 → Propiedades → DNS preferido `10.37.7.33`. Ejecuta en CMD o PowerShell:

```powershell
ipconfig /all
nslookup ns1.p07-l03.sri.test 10.37.7.33
Resolve-DnsName ns1.p07-l03.sri.test -Server 10.37.7.33 -Type A
```

**Comparación de pruebas:** `nslookup/dig @IP` consulta servidor explícito; `getent` y navegador siguen la configuración NSS/resolved/hosts/caché del SO. No confundas un hosts local con respuesta del servidor BIND.

<a id="secundario"></a>
## 8 · Segundo servidor y transferencias (fase avanzada · 3 VM)

1. Clona la VM base (nueva MAC). `dns2-p07-l03` con IP fija `10.37.7.43` y **Red Interna SRI-P07**.
2. Instala `bind9 bind9-utils dnsutils` en dns2.
3. En primario, después de comprobar que el secundario existe, añade a zona directa y a inversa un NS `ns2`, un A `ns2 10.37.7.43` en directa y PTR `43` en inversa. **Incrementa serial de ambas**.
4. En primario, para cada `zone` sustituye `allow-transfer { none; };` por `allow-transfer { 10.37.7.43; };` y añade `also-notify { 10.37.7.43; };`.
5. En **dns2** declara la zona directa:

```conf
zone "p07-l03.sri.test" {
    type secondary;
    primaries { 10.37.7.33; };
    file "/var/cache/bind/db.p07-l03.sri.test";
};
```

6. Declara asimismo `7.37.10.in-addr.arpa` con `primaries { 10.37.7.33; };` y archivo propio bajo `/var/cache/bind/`.
7. Valida `sudo named-checkconf`, `sudo systemctl restart named` (o unidad real). Comprueba desde cliente `dig @10.37.7.43 ns1.p07-l03.sri.test A +noall +answer` y transferencia autorizada desde secundario `dig @10.37.7.33 p07-l03.sri.test AXFR` (en dns2).
8. Detén DNS primario y consulta **explícitamente** al secundario. **No presupongas** que el cliente cambia de resolver por arte de magia: configura ambos DNS/observa timeout antes de afirmar tolerancia a fallos.

**`type secondary` conserva una copia de la zona, no crea una segunda zona primaria independiente.** No copies ficheros manualmente y lo llames transferencia. En producción protege transferencias (ACL + TSIG).

<a id="cache"></a>
## 9 · Caché, recursión y reenviadores: separar DNS de Internet

**Fase 1 offline:** `dig @10.37.7.33 www.p07-l03.sri.test` consulta registros **locales**. Para comparar caché recursiva de dominios públicos hace falta que el servidor alcance resolutores externos mediante NAT u otra ruta real y que la política lo permita.

En dns1, edita **dentro del bloque options** de `/etc/bind/named.conf.options` (no crees un segundo bloque `options`):

```conf
recursion yes;
allow-recursion { 127.0.0.1; 10.37.7.0/24; };
allow-query-cache { 127.0.0.1; 10.37.7.0/24; };
```

Si **hay salida por NAT y se permite**:

```conf
forwarders { 9.9.9.9; 1.1.1.1; };
forward first;
```

Si es un laboratorio **completamente aislado**, omite forwarders; no prometas `dig google.es` funcional. `forward first` puede intentar resolución iterativa si fallan upstreams; `forward only` no. Comprueba:

```bash
sudo named-checkconf
sudo rndc reconfig
dig @10.37.7.33 example.org A +stats
dig @10.37.7.33 example.org A +stats
```

No concluyas «segundo dig siempre 0 ms»: latencia depende de caché, proceso y red. **Resolver un nombre externo ≠ tener enrutamiento/Internet desde el cliente**; `dig` puede funcionar aunque ICMP al destino falle.

<a id="delegacion"></a>
## 10 · Delegación y servidor de una subzona (fase profesional)

Ejemplo: delegar `dev.p07-l03.sri.test` a un servidor DNS separado `.43`. **NO equivale a configurar dns2 como secundario de la zona padre**: son dos prácticas/roles distintos (usa snapshots separados).

- Padre `p07-l03.sri.test`: añade `dev IN NS nsdev.p07-l03.sri.test.` y `nsdev IN A 10.37.7.43` (glue cuando proceda). Sube serial y recarga.
- Servidor delegado `.43`: zona primaria `dev.p07-l03.sri.test`, `@ NS nsdev.p07-l03.sri.test.`, `nsdev A .43`, `app A 10.37.7.73`.
- Comprobación desde cliente `dig @10.37.7.33 app.dev.p07-l03.sri.test A +trace` puede depender de Internet/root; en LAB pregunta a la autoridad padre y subzona con `dig @IP`, comprueba referencia NS y resuelve mediante recursión interna configurada.

El material histórico de Fran distingue **primario, secundario, caché, delegación, DDNS y hosts**; los conservamos, pero no reutilizamos su zona/IPv4 literal `192.168.1.0/24` ni la confusión «DNS funciona → ping Internet funciona».

<a id="ddns"></a>
## 11 · DDNS con Kea: qué queda integrado y qué no

**Paso 1:** en UT01 Kea entrega opción 6=`10.37.7.33` y 15=`p07-l03.sri.test`; una lease correcta **no crea un A/PTR**.

> **Al pasar de IP manual a DHCP:** `cli.p07-l03.sri.test A 10.37.7.123` era el registro del cliente MANUAL de la primera fase. Si el cliente obtiene otra IP del pool Kea, ese A/PTR no cambia por arte de magia: actualiza manualmente el registro en la fase guiada o implanta DDNS de forma segura en la ampliación. Para verificar primero el DNS fijo, consulta `ns1` en `.33`.

**Paso 2:** DDNS auténtico necesita un actualizador (Kea DHCP-DDNS/D2 u otra implementación) y permisos limitados en BIND. No se habilita `allow-update { any; };` para «hacerlo funcionar». Usa TSIG y zona escribible en ruta autorizada (`/var/lib/bind/`), con backups y control de cambios. Consulta [Kea DHCP-DDNS](https://kea.readthedocs.io/) y la documentación BIND de actualizaciones dinámicas antes del laboratorio avanzado.

**Paso 3:** verifica con `dig @10.37.7.33 <cliente>.p07-l03.sri.test A` **antes y después de una adquisición realmente nueva**, correlacionando lease, log del actualizador y registro nuevo. Si se trabaja solo el diseño, etiquétalo como **diseño**, no como DDNS ejecutado.

<a id="diagnostico"></a>
## 12 · Fallos habituales y comprobaciones

| Síntoma | Comprobación que separa causas |
|---|---|
| `dig @.33` timeout | NIC/red interna/servicio/puerto 53/ACL. `ip -br a`, `systemctl`, `ss -lntup`. |
| `SERVFAIL` | Logs, forwarders, DNSSEC, zona no cargada. `journalctl`, `named-checkconf`, `named-checkzone`. |
| `NXDOMAIN` | Dominio o owner inexistente, zona equivocada, caché negativa. Prueba autoridad exacta. |
| `NOERROR` sin A | Existe el nombre, pero no RR del tipo solicitado; compara A/CNAME/MX. |
| Solo `dig @IP` responde | DNS predeterminado cliente/hosts/NSS/resolved; no cambies BIND sin probar. |
| PTR falla y A responde | Falta zona inversa o PTR; son bases de datos distintas. |
| Secundario no actualiza | ACL transferencia, TCP/53, serial, primaries, logs. |
| Consulta externa falla | NAT/ruta/reenviador/política recursiva; no invalida zona local. |

Secuencia de trabajo: **síntoma → hipótesis → prueba → evidencia → cambio mínimo → misma prueba → rollback**. No tocar zona, DNS cliente y cortafuegos simultáneamente.

<a id="practicas"></a>
## 13 · Ruta de aprendizaje y prácticas evaluables

1. DNS01: arquitectura y zona directa/inversa con 2 VM.
2. DNS02: registros, cliente heterogéneo y hosts.
3. DNS03: primario/secundario y transferencias (3 VM).
4. DNS04: caché/delegación/DDNS (ampliación por etapas; no presupongas Internet).
5. DNS05: Windows Server 2025 en LAN `SRI-W-Pxx`.
6. DNS06: diagnóstico comparado, copia y defensa.

**El tutorial proporciona valores de ejemplo; la tarea usa tu variante individual y una modificación que tendrás que justificar.** Evaluamos consultas reales, serial, registros/zonas y documentación, no el número de capturas. Descarga los enunciados del aula virtual.

### Referencias técnicas

- [BOE · módulo 0375, RA1](https://www.boe.es/buscar/doc.php?id=BOE-A-2009-18355)
- [Debian · BIND9](https://wiki.debian.org/BIND9)
- [ISC · BIND9 ARM](https://bind9.readthedocs.io/)
- [Debian · named-checkconf](https://manpages.debian.org/trixie/bind9-utils/named-checkconf.1.en.html)
- [Debian · named-checkzone](https://manpages.debian.org/trixie/bind9-utils/named-checkzone.1.en.html)
- [Microsoft Learn · DNS Windows](https://learn.microsoft.com/es-es/windows-server/networking/dns/quickstart-install-configure-dns-server)
